---
import { getEntry, render } from 'astro:content';
import Layout from '../../components/Layout.astro';
import { t } from '../../i18n/ui';

const page = await getEntry('paginas', 'es/contacto');
const { Content } = await render(page!);
---

<Layout lang="es" title={page!.data.title} current="contacto" path="/contacto/">
  <div class="prose" style="margin-top: 3rem;">
    <h1>{page!.data.title}</h1>
    <Content />
    <div class="contact-actions">
      <a class="button" href="mailto:negracuarenta@gmail.com">{t('es', 'email_us')}</a>
    </div>
    <p>
      {t('es', 'find_us')}
      <!-- TODO: reemplazar por las URLs reales de Facebook / Instagram / Vimeo -->
      <a href="#">Facebook</a>, <a href="#">Instagram</a> y <a href="#">Vimeo</a>.
    </p>
  </div>
</Layout>
